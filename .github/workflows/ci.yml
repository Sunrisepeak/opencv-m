name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  MCPP_VERSION: "0.0.101"

jobs:
  # ── Platform × toolchain matrix ─────────────────────────────────────────
  # Every leg source-builds the vendored OpenCV 5 (+ compat.ffmpeg) cold:
  # ~2700 TUs. The ~/.mcpp/registry cache carries toolchains and the built
  # compat.ffmpeg across runs; timeout is sized for the COLD path.
  #
  # experimental legs (continue-on-error):
  #   linux-musl  — green locally; blocked in CI on mcpp-index#110
  #                 (compat.ffmpeg -Wno-error=incompatible-pointer-types for
  #                 musl's ioctl signature). Flip to required when it merges.
  #   windows     — parked on mcpp-community/mcpp#258 (the manifest cannot
  #                 express windows' per-glob define deltas); build.mcpp
  #                 fails fast with that pointer. gen/windows/ snapshots and
  #                 the parked flag tables are already committed.
  test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 150
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-gcc (required)
            os: ubuntu-latest
            suffix: linux-x86_64
            ext: tar.gz
            mcpp: bin/mcpp
            setup: |
              mcpp toolchain install gcc 16.1.0
              mcpp toolchain default gcc@16.1.0
            experimental: false
            features: true
            examples: true
          - name: linux-llvm-22
            os: ubuntu-latest
            suffix: linux-x86_64
            ext: tar.gz
            mcpp: bin/mcpp
            setup: |
              mcpp toolchain install llvm 22.1.8
              mcpp toolchain default llvm@22.1.8
            experimental: false
            features: false
            examples: false
          - name: linux-musl-static (pending mcpp-index#110)
            os: ubuntu-latest
            suffix: linux-x86_64
            ext: tar.gz
            mcpp: bin/mcpp
            setup: |
              mcpp toolchain install gcc 16 --target x86_64-linux-musl
              mcpp toolchain default gcc@16.1.0 --target x86_64-linux-musl
            experimental: true
            features: false
            examples: false
          - name: macos-llvm
            os: macos-15
            suffix: macosx-arm64
            ext: tar.gz
            mcpp: bin/mcpp
            setup: ""   # mcpp resolves its macOS default (llvm) itself
            experimental: false
            features: false
            examples: false
          - name: windows (parked on mcpp#258)
            os: windows-latest
            suffix: windows-x86_64
            ext: zip
            mcpp: bin/mcpp.exe
            setup: ""
            experimental: true
            features: false
            examples: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore mcpp registry cache
        uses: actions/cache@v4
        with:
          # Holds toolchains AND built deps (compat.ffmpeg, gtest), so a
          # repeat run rebuilds little. Keyed per leg; prefix-restores across
          # manifest changes.
          path: ~/.mcpp/registry
          key: mcpp-registry-${{ matrix.name }}-${{ env.MCPP_VERSION }}-${{ hashFiles('mcpp.toml', '.github/workflows/ci.yml') }}
          restore-keys: |
            mcpp-registry-${{ matrix.name }}-${{ env.MCPP_VERSION }}-

      - name: Download mcpp
        shell: bash
        env:
          MCPP_ARCHIVE: mcpp-${{ env.MCPP_VERSION }}-${{ matrix.suffix }}.${{ matrix.ext }}
          MCPP_ROOT: mcpp-${{ env.MCPP_VERSION }}-${{ matrix.suffix }}
        run: |
          curl -L -fsS -o "$MCPP_ARCHIVE" \
            "https://github.com/mcpp-community/mcpp/releases/download/v${MCPP_VERSION}/${MCPP_ARCHIVE}"
          case "$MCPP_ARCHIVE" in
            *.zip) powershell -NoProfile -Command "Expand-Archive -Force -Path '${MCPP_ARCHIVE}' -DestinationPath '.'" ;;
            *)     tar -xzf "$MCPP_ARCHIVE" ;;
          esac
          root="$PWD/$MCPP_ROOT"
          mkdir -p "$HOME/.mcpp/registry"
          cp -a "$root/registry/." "$HOME/.mcpp/registry/"
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            echo "$(cygpath -m "$root/bin")" >> "$GITHUB_PATH"
          else
            echo "$root/bin" >> "$GITHUB_PATH"
          fi

      - name: Set up toolchain
        if: matrix.setup != ''
        shell: bash
        run: |
          ${{ matrix.setup }}
          mcpp --version

      - name: Template placeholder lint
        if: matrix.features
        shell: bash
        # mcpp's renderer knows exactly {{project.name}} / {{self.name}} /
        # {{self.version}} — anything else ships literally into scaffolds.
        run: |
          bad=$(grep -rno '{{[^}]*}}' templates/ | grep -v -E '\{\{(project\.name|self\.name|self\.version)\}\}' || true)
          if [ -n "$bad" ]; then echo "unknown template placeholders:"; echo "$bad"; exit 1; fi

      - name: Build
        shell: bash
        run: mcpp build

      - name: Test
        shell: bash
        run: mcpp test

      - name: Feature tests (dnn + unifont module interfaces)
        if: matrix.features
        shell: bash
        run: mcpp test --features dnn,unifont

      - name: Run probe example (path-dep consumer)
        if: matrix.examples
        shell: bash
        run: |
          cd examples/probe
          mcpp run

      - name: Run gray_pipeline example
        if: matrix.examples
        shell: bash
        run: |
          cd examples/gray_pipeline
          mcpp run
