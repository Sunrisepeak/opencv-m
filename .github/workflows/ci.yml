name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linux:
    name: mcpp (ubuntu-latest, required)
    runs-on: ubuntu-latest
    # compat.opencv now source-builds OpenCV 5 AND pulls compat.ffmpeg
    # transitively for the videoio backend, both from source on a cold runner
    # (the ~/.mcpp registry is not cached here). 90 leaves headroom over the
    # ~50-min cold build; the default feature set excludes dnn, so this is the
    # lean base+videoio profile, not the heavy dnn variant.
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # No apt nasm: compat.opencv's NASM .asm units (jpeg-turbo SIMD, core AVX)
      # are covered by mcpp's synchronous nasm bootstrap via xlings (mcpp#232,
      # floor 0.0.98) — the same drop already proven in mcpp-index and ffmpeg-m.
      - name: Install Xlings
        env:
          XLINGS_NON_INTERACTIVE: 1
        run: |
          curl -fsSL https://raw.githubusercontent.com/openxlings/xlings/refs/heads/main/tools/other/quick_install.sh | bash
          echo "$HOME/.xlings/subos/current/bin" >> "$GITHUB_PATH"

      - name: Install project tools
        run: |
          xlings update
          xlings install

      - name: Set default toolchain
        run: |
          mcpp toolchain install gcc 16.1.0
          mcpp toolchain default gcc@16.1.0
          mcpp --version

      - name: Build library (via compat.opencv source build)
        run: mcpp build

      - name: Run tests (API surface + macros mix + codec roundtrips)
        run: mcpp test

      - name: Run probe example (path-dep consumer)
        run: |
          cd examples/probe
          mcpp run

      - name: Run gray_pipeline example
        run: |
          cd examples/gray_pipeline
          mcpp run
